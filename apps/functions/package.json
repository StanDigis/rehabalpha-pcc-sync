{
  "name": "@rehabalpha/functions",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloud Functions: webhook ingest, task worker, reconciliation, operator actions.",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@google-cloud/tasks": "^7.0.0",
    "@rehabalpha/core": "*",
    "@rehabalpha/pcc-client": "*",
    "@rehabalpha/sync": "*",
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.2",
    "zod": "^4.4.3"
  }
}
